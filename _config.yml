theme: jekyll-theme-minimal
language: node_js
node_js:
  - 'stable'
sudo: false
env:
  - workerCount=3
matrix:
  fast_finish: true
branches:
  only:
  - master
install:
  - npm uninstall typescript
  - npm install
cache:
  directories:
    - node_modules
git:
  depth: 1